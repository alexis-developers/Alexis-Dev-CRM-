version: '3.8'

services:
  wacrm:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: wacrm-app
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    restart: unless-stopped
